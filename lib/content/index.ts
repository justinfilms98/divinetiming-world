/**
 * Content shared utilities and types — safe for Client and Server.
 * For server-only fetchers (getHeroSection, getEvents, etc.) import from '@/lib/content/server'.
 */

export { parseYouTubeId, PAGE_SLUGS } from './shared';
export type { MediaPageVideo, GalleryForHub, PageSettings, HeroSection, HeroCarouselSlide, Event, Gallery, GalleryMedia, Product, BookingContentSection } from './shared';
